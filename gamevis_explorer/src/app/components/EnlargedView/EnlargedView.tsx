import { Annotation } from "../../types/types";

export const EnlargedView = ({
  data,
  yDimension,
  xDimension,
  handler,
}: {
  data: Annotation[];
  yDimension: string;
  xDimension: string;
  handler: () => void;
}) => {
  return <></>;
};
